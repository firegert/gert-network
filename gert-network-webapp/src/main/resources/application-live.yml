management:
  server:
    port: ${SERVER_MGMT_PORT:8081}

spring:
  datasource:
    hikari:
      maximum-pool-size: 15
      minimum-idle: 7
      connection-timeout: 60000
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show-sql: false
        format-sql: false
